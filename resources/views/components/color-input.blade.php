<div class="flex gap-4 items-center justify-start">
    <label for="{{ $name }}">{{ $label }}</label>
    <input class="border-gray-200 border-2 rounded-3xl px-4 py-1" id="{{ $id }}" name="{{ $name }}" type="color"
        placeholder="{{ $placeholder }}" value="{{ $value }}" />
</div>